program main{
        var i,n,f,x : int;
        begin;
                write("texto");
                if(3>2) then
                {
                    i := 2 * 2 - 3;
                    write(i);
                }
                f := 5 * 2 + 4 / 2;
                x := 1 + 1;
                write(f);
                write(x);
        end;
}
