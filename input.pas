program main{
        var i,n,f,x : int;
        begin;
                write("texto");
                f := 5 * 2 + 4 / 2;
                x := 1 + 1;
                n := f + 1;
                write(f);
                write(x);
                if(2 > 1) then {
                    i := 1;
                    write("trueif");
                    if(6 > 1) then {
                        write("trueifdos");
                    }
                }
                if(2 < 1) then {
                    write("falseif");
                }

        end;
}
