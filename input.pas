program main{
        var i,n,f,x : int;
        begin;
                f := 5 * 2 + 4 / 2;
                x := 4 + 2;
                write(f);
                write(x);
                write("texto");
                n := f + x;
        end;
}
