program main{
        var i,n,f,x : int;
        begin;
                write("texto");
                f := 5 * 2 + 4 / 2;
                x := 4;
                n := f + 1;
                write(f);
                if(f<x) then
                {
                    write("istate");
                }

        end;
}
